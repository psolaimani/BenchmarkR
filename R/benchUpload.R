# benchUpload(info=c(TIMINGS, META, BENCHMARKS),type="DB",con=c(user,pass,url))
# allows upload of timings/benchmarks/meta data to database

benchUpload <- function(info,type="DB",con){


  cat("\n\nDATA uploaded to database.\n")
}
